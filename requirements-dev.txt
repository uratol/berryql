# Development dependencies for BerryQL
# Include core dependencies
-r requirements-core.txt

# Async database adapters
aiosqlite>=0.17.0
asyncpg>=0.28.0  # PostgreSQL adapter (now working with Windows event loop policy)
aioodbc>=0.5.0  # SQL Server adapter
pyodbc>=5.2.0   # ODBC driver for SQL Server

# Environment variable support
python-dotenv>=1.0.0

# Testing framework
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0

# Code quality and formatting
black>=22.0.0
isort>=5.10.0
mypy>=1.0.0
flake8>=4.0.0
pre-commit>=2.15.0

# Type stubs for better type checking
types-setuptools
