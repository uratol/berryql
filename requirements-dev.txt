# Development and testing dependencies for BerryQL
# Pull in runtime requirements
-r requirements-core.txt

# Async DB adapters used in tests and examples (optional)
aiosqlite>=0.19.0
asyncpg>=0.29.0
# SQL Server (optional, only if you have drivers installed)
pyodbc>=5.2.0

# Env utilities
python-dotenv>=1.0.1

# Testing
pytest>=8.2
pytest-asyncio>=0.23
pytest-cov>=5.0

# Linting/formatting/type-checking
black>=24.3
isort>=5.13
mypy>=1.10
flake8>=7.0
pre-commit>=3.7

# Build and publish
build>=1.2.1
twine>=5.0.0
check-manifest>=0.50

# Type stubs
types-setuptools
